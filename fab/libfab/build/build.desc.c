/* A desc definition file generated by fab. fab is free software released under the
   GNU General Public License.

   As a special exception, desc definition files generated by fab (including this
   desc definition file) are excluded from the license covering fab itself, even if
   substantial portions of the fab source code are copied verbatim into the desc
   definition file. You may create a larger work that contains part or all of the build
   script and distribute that work under terms of your choice. */

#include "descriptor.h"
#include "build/build.h"
#include "build.desc.h"
#include "macros.h"

struct descriptor_type API descriptor_fab_build = {
  members_len : 3,
  members : (descriptor_field*[]) {
    &descriptor_fab_build_state,
    &descriptor_fab_build_numranks,
    &descriptor_fab_build_numexecs,
  }
};

struct descriptor_field API descriptor_fab_build_state = { id : 1, code : 1
  , offset : offsetof(fab_build, state)
  , size : sizeof_member(fab_build, state)
};
struct descriptor_field API descriptor_fab_build_numranks = { id : 2, code : 2
  , offset : offsetof(fab_build, numranks)
  , size : sizeof_member(fab_build, numranks)
};
struct descriptor_field API descriptor_fab_build_numexecs = { id : 3, code : 2
  , offset : offsetof(fab_build, numexecs)
  , size : sizeof_member(fab_build, numexecs)
};
