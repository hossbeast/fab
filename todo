* v0.6
 xapi
  tune?
  use libnarrate everywhere
  use xapi/enter everywhere
  use libxapi/error everywhere (remove custom error tables)
  cleanup libxapi commented out code
  libxapi unit test for prologue
  libxapi xproxy unit tests

* finished

* docket
 listwise resolve lstack vs lwx
 listwise resolve write vs alt_write
 listwise ordered access
 listwise generator -> transform
 listwise sanity checks

 per-filesystem options (canon, hashblock stat vs content)
 configurable options (fabrc, override cmdline)

 makescripts.sh should not use ./fab/fab.devel, because then you cant make changes that break the current version
  it should build with either 1) system fab, or 2) a configurable directory

** profiling
