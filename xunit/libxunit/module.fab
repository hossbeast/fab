# this module contains C sources
use c
use so

# make common objects available for #inclusion and linking by files in this module
#require common

# libraries
#require libxapi, libxlinux, liblogger

# this module produces three artifacts
artifact libxunit{.xapi.{final,debug,devel}}?.so

# dependencies for artifacts
rule libxunit?.so : libxapi?.so, libxlinux?.so, liblogger?.so, exports
