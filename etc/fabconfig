# file invalidation
filesystems : {
  "/" : {
    invalidate : notify
  }
}

# heuristic based on detected cpus
workers : {
  concurrency : 0
}

# heuristic based on detected cpus
build : {
  concurrency : 0
}

# formula execution $PATH
formula : {
  path : {
    dirs : {
      /usr/bin
    }
  }
}

special : {
  module : "module.bam"
  model : "model.bam"
  var : "var.bam"
  config : "config.bam"
}

walker : {
  # external modules
  include : {
    /usr/share/fab/modules
  }

  # except skip this stuff
  exclude : {
    /home/todd/builtin-modules
    .git
  }
}
